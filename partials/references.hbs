{{#if resume.references}}
{{#>section title=(sectionTitle resume "references" "References") class="references-title"}}
	{{#each resume.references}}
	<blockquote class="mb-2">
		{{this.reference}}
		<br />
		─ {{this.name}}
	</blockquote>
	{{/each}}
{{/section}}
{{/if}}